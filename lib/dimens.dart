/// Constantes de dimensions utilisées dans toute l'application.
/// Modifier ici pour impacter l'ensemble de l'UI.
abstract final class Dimens {
  // ── Espacements ────────────────────────────────────────────────────────────
  static const double spaceXxs = 3.0;
  static const double spaceXs  = 4.0;
  static const double spaceS   = 6.0;
  static const double spaceM   = 8.0;
  static const double spaceL   = 12.0;
  static const double spaceXl  = 16.0;
  static const double spaceXxl = 20.0;
  static const double space24  = 24.0;
  static const double space32  = 32.0;
  static const double space40  = 40.0;

  // ── Padding cards / sections ───────────────────────────────────────────────
  static const double cardPadding     = spaceXl;   // 16
  static const double screenPadding   = spaceXxl;  // 20
  static const double homePadding     = space40;   // 40

  // ── Hauteurs de boutons ────────────────────────────────────────────────────
  static const double buttonHeightM   = 48.0;
  static const double buttonHeightL   = 56.0;

  // ── Largeurs max (layout centré) ───────────────────────────────────────────
  static const double maxWidthHome    = 600.0;
  static const double maxWidthReview  = 700.0;

  // ── Icônes ─────────────────────────────────────────────────────────────────
  static const double iconXs  = 14.0;
  static const double iconS   = 16.0;
  static const double iconM   = 18.0;
  static const double iconL   = 20.0;
  static const double iconXl  = 24.0;
  static const double iconHero = 60.0;

  // ── Border radius ──────────────────────────────────────────────────────────
  static const double radiusXs  = 4.0;
  static const double radiusS   = 6.0;
  static const double radiusM   = 8.0;
  static const double radiusL   = 10.0;
  static const double radiusXl  = 12.0;
  static const double radiusXxl = 16.0;
  static const double radiusHero = 30.0;

  // ── Divers ─────────────────────────────────────────────────────────────────
  static const double progressBarHeight  = 8.0;
  static const double homeIconSize       = 120.0;
  static const double infoLabelWidthHome = 100.0;
  static const double infoLabelWidthSettings = 120.0;
}


